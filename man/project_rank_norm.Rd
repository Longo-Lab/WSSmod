% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/project_rank_norm.R
\name{project_rank_norm}
\alias{project_rank_norm}
\title{Project New Values onto a Frozen Rank-Normal Reference}
\usage{
project_rank_norm(train_vals, new_vals, k = 0.375)
}
\arguments{
\item{train_vals}{Numeric vector, the frozen reference distribution
(e.g. a baseline cohort's biomarker values). Must not contain \code{NA}.}

\item{new_vals}{Numeric vector of new values to project onto
\code{train_vals}. May be a single value.}

\item{k}{Blom-type constant, passed through to match
\code{\link[RNOmni:RankNorm]{RNOmni::RankNorm()}}'s default.}
}
\value{
A numeric vector the same length as \code{new_vals}, giving each
value's projected normal quantile.
}
\description{
Projects \code{new_vals} onto the rank-based inverse-normal transform defined
by \code{train_vals}, without ever refitting or observing \code{new_vals} when
determining the transform. Each new value is treated as a single
insertion into the frozen, sorted \code{train_vals} reference: its rank is the
count of reference values at or below it (ties in \code{train_vals} share the
same rank), and that rank is converted to a normal quantile using the
same Blom-type constant (\code{k = 0.375}) as \code{\link[RNOmni:RankNorm]{RNOmni::RankNorm()}}.
}
\details{
This is useful for scoring new samples (including a single new sample)
against a fixed reference cohort without leaking information from the
new samples into the reference's distribution -- e.g. avoiding the
train/test leakage that results from rank-normalizing a pooled
reference+new-sample dataset together.

Note this does \strong{not} reproduce \code{RNOmni::RankNorm(train_vals)}
bit-for-bit when \code{new_vals} happens to equal \code{train_vals}: it always
treats each new value as an \emph{additional} observation inserted into the
\code{n}-sample reference (using \code{n + 1} in the denominator), rather than as
one of the original \code{n} samples. The two are close but not identical.
}
\examples{
train_vals <- rnorm(200)
project_rank_norm(train_vals, new_vals = c(-1, 0, 1))

}
\seealso{
\code{\link[=normalize_wss_biomarkers]{normalize_wss_biomarkers()}}
}
