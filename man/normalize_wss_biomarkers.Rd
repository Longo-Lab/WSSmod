% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/normalize_wss_biomarkers.R
\name{normalize_wss_biomarkers}
\alias{normalize_wss_biomarkers}
\title{Normalize Raw Biomarker Values for predict_WSS()}
\usage{
normalize_wss_biomarkers(
  raw_biomarkers,
  prebuilt = "core_AD_plasma_biomarkers",
  method = c("project", "self")
)
}
\arguments{
\item{raw_biomarkers}{A data.frame or matrix, one row per sample, with
columns named by the raw (unsuffixed) biomarker names (e.g.
\code{PlasmaPTau181}, not \code{PlasmaPTau181_norm}).}

\item{prebuilt}{Name of a prebuilt reference set to use when
\code{method = "project"}. See \code{\link[=list_prebuilt_wss]{list_prebuilt_wss()}} for available options.
Ignored when \code{method = "self"}.}

\item{method}{Normalization method:
\describe{
\item{\code{"project"}}{(default) Project each raw value onto a bundled
reference distribution via \code{\link[=project_rank_norm]{project_rank_norm()}}. Works for a
single sample or a small/differently-distributed cohort, since
each value is scored independently against the fixed reference.}
\item{\code{"self"}}{Rank-normalize (via \code{\link[RNOmni:RankNorm]{RNOmni::RankNorm()}}) within
\code{raw_biomarkers} itself. Appropriate if you have a sizeable cohort
of your own with a distribution you're comfortable normalizing
against directly, instead of the bundled reference. Requires at
least two samples, since a rank transform needs multiple values to
rank against.}
}}
}
\value{
A data.frame with one normalized column per input biomarker
column (same names as \code{raw_biomarkers}), row names preserved. Combine
with \code{Age}/\code{Gender} columns and pass to \code{\link[=predict_WSS]{predict_WSS()}}.
}
\description{
Builds the rank-based inverse-normal-transformed biomarker columns
\code{\link[=predict_WSS]{predict_WSS()}} requires, from raw biomarker values, using one of two
methods. Output columns keep the same names as the input columns (e.g.
\code{PlasmaPTau181} stays \code{PlasmaPTau181}) -- \code{\link[=predict_WSS]{predict_WSS()}} reads whatever
column names its underlying model actually expects.
}
\examples{
raw_biomarkers <- data.frame(PlasmaPTau181 = 1.5, PlasmaNfL = 20)
normalize_wss_biomarkers(raw_biomarkers)

}
\seealso{
\code{\link[=predict_WSS]{predict_WSS()}}, \code{\link[=project_rank_norm]{project_rank_norm()}},
\code{\link[=load_prebuilt_biomarker_reference]{load_prebuilt_biomarker_reference()}}
}
