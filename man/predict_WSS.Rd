% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict_WSS.R
\name{predict_WSS}
\alias{predict_WSS}
\title{Predict WSS Module Scores from Core Biomarkers}
\usage{
predict_WSS(newdata, prebuilt = "core_AD_plasma_biomarkers", type = "response")
}
\arguments{
\item{newdata}{A data.frame or matrix with one row per sample and (at
least) the columns required by the model; see \code{x_cols} in
\code{\link[=load_prebuilt_wss_model]{load_prebuilt_wss_model()}} for the exact set and order. For
\code{"core_AD_plasma_biomarkers"} this is \code{Age} (raw years), \code{Gender}
(numeric, \code{1} = male, \code{0} = not male), and the 8 core biomarkers
(\code{PlasmaPTau181}, \code{PlasmaAB142P}, \code{PlasmaAB140P}, \code{PlasmaABRatio},
\code{PlasmapTau217}, \code{PlasmapTau217_AB42Ratio}, \code{PlasmaGFAP}, \code{PlasmaNfL}).

\strong{The biomarker columns must already be rank-based inverse-normal
transformed before calling this function} -- see
\code{\link[=normalize_wss_biomarkers]{normalize_wss_biomarkers()}} to build them from raw biomarker values,
including for a single new patient.}

\item{prebuilt}{Name of a prebuilt reference set. See
\code{\link[=list_prebuilt_wss]{list_prebuilt_wss()}} for available options.}

\item{type}{Prediction type passed to \code{predict.joinet()}: \code{"response"}
(the default) or \code{"link"}.}
}
\value{
A list with components \code{base} (first-layer-only predictions) and
\code{meta} (final stacked predictions), each a matrix with one row per
sample (row names taken from \code{newdata}, if present) and one column per
module (named by the model's \code{outcomes}).
}
\description{
Predicts module scores for a prebuilt reference set's \code{joinet} model (a
two-layer elastic net stack) without needing to measure the full
proteomic panel. For \code{"core_AD_plasma_biomarkers"}, this predicts all 75
module scores from Age, Gender, and the 8 core plasma biomarkers used in
the original analysis.
}
\examples{
\dontrun{
# raw_biomarkers: one row per sample, the 8 raw (un-normalized) biomarker
# columns. Works for a single patient too.
normalized <- normalize_wss_biomarkers(raw_biomarkers)

newdata <- cbind(Age = ages, Gender = genders, normalized)
pred <- predict_WSS(newdata)
pred$meta
}

}
\seealso{
\code{\link[=normalize_wss_biomarkers]{normalize_wss_biomarkers()}}, \code{\link[=load_prebuilt_wss_model]{load_prebuilt_wss_model()}},
\code{\link[=calculate_WSS]{calculate_WSS()}}
}
