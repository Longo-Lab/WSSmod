% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prebuilt_wss.R
\name{load_prebuilt_biomarker_reference}
\alias{load_prebuilt_biomarker_reference}
\title{Load a Prebuilt Biomarker Reference Distribution}
\usage{
load_prebuilt_biomarker_reference(prebuilt = "core_AD_plasma_biomarkers")
}
\arguments{
\item{prebuilt}{Name of a prebuilt reference set. See
\code{\link[=list_prebuilt_wss]{list_prebuilt_wss()}} for available options.}
}
\value{
A data.table with columns \code{biomarker} and \code{value} (long format,
one row per reference observation; biomarkers have differing numbers of
rows due to differing missingness in the original cohort), or \code{NULL} if
the given reference set has no associated biomarker reference.
}
\description{
Some prebuilt reference sets ship a baseline biomarker reference
distribution used by \code{\link[=normalize_wss_biomarkers]{normalize_wss_biomarkers()}} (\code{method = "project"})
to rank-normalize new raw biomarker values without needing a sizeable
cohort of your own. Each biomarker's reference values are drawn from the
real baseline cohort, but \strong{independently randomly shuffled} so that no
two values across different biomarkers can be attributed to the same
original subject. This preserves each biomarker's marginal distribution
exactly (every value, tie, and extreme is real) while destroying the
joint/multivariate fingerprint that carries re-identification risk.
}
\details{
Because of this, \strong{the returned reference has no valid joint structure}
— inter-biomarker correlations are deliberately destroyed. Only use it
for independent per-biomarker operations (rank/quantile lookups), never
for anything requiring multivariate relationships (e.g. correlation or
joint modeling).
}
\seealso{
\code{\link[=normalize_wss_biomarkers]{normalize_wss_biomarkers()}}, \code{\link[=project_rank_norm]{project_rank_norm()}}
}
